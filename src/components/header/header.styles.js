import styled from 'styled-components';

const StyledTitle = styled.h1`
	color: black;
	text-align: center;
`;
const StyledHeader = styled.header`
	color: black;
	display: flex;
	justify-content: space-between;
	margin-bottom: 15px;
`;

export { StyledTitle, StyledHeader };
